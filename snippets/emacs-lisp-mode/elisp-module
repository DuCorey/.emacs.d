# -*- mode: snippet -*-
# name: elisp-module
# key: module
# --
;;; `(file-name-base (buffer-file-name))`.el --- $1

;;; Commentary:

;;; Code:

$0

(provide '`(file-name-base (buffer-file-name))`)

;;; `(file-name-base (buffer-file-name))`.el ends here